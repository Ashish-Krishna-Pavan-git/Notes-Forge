Windows desktop music source files live here.

- `manifest.json` defines the playlist for the Windows package.
- media files in this folder are copied into `webapp/frontend/dist/music` during the Windows build.
