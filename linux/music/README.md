Linux desktop music source files live here.

- `manifest.json` defines the playlist for the Linux package.
- media files in this folder are copied into `webapp/frontend/dist/music` during the Linux build.
